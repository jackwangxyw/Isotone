import QtQuick
import Isotone

// A Settings page's section header: the prototype's .set h6 (13.5 px, 600,
// 28 px above, 6 below).
Text {
    topPadding: 28
    bottomPadding: 6
    font.family: Theme.font
    font.pixelSize: 13
    font.weight: Font.DemiBold
    color: Theme.text
}
