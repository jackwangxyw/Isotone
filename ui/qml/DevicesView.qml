import QtQuick
import Isotone

// The Devices view (Devices board): every output, its engine and status, and
// the selected output's detail with its actions.
// FOUNDATION PLACEHOLDER: the devices work package builds it.
Item {
    id: root
    Text {
        x: 36
        y: 30
        text: "Devices"
        font.family: Theme.font
        font.pixelSize: 26
        font.weight: Font.DemiBold
        font.letterSpacing: -0.39
        color: Theme.text
    }
}