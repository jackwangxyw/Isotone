import QtQuick
import Isotone

// The presets popover (PresetsMenu board), from the preset name.
// FOUNDATION PLACEHOLDER: the presets work package builds it. Main opens it with
// openAt(x, y), the popover's top left in window coordinates.
Popover {
    id: root
    panelWidth: 400
}