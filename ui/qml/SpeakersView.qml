import QtQuick
import Isotone

// The Speakers view (Speakers board), for outputs with more than two channels.
// FOUNDATION PLACEHOLDER: the surround work package builds it.
Item {
    id: root
    Text {
        x: 36
        y: 30
        text: "Speakers"
        font.family: Theme.font
        font.pixelSize: 26
        font.weight: Font.DemiBold
        font.letterSpacing: -0.39
        color: Theme.text
    }
}